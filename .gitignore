.DS_Store
.server-info
.server.pid
android/
ios/
node_modules/

# --- Antigravity / AI Agent Config ---

# 1. Ignore the "Trash" (Temporary session data, logs, and cache)
.agent/cache/
.agent/logs/
.agent/sessions/
.agent/temp/

# 2. Keep the "Brain" (The instructions you actually want)
!.agent/rules/
!.agent/skills/
!.agent/workflows/